# The main pipeline for ecom_bronze_ingestion_pipeline_bundle

resources:
  pipelines:
    ecom_bronze_ingestion_pipeline_bundle_etl:
      name: ecom_bronze_ingestion_pipeline_bundle_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/ecom_bronze_ingestion_pipeline_bundle_etl"

      libraries:
        - glob:
            include: ../src/ecom_bronze_ingestion_pipeline_bundle_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
